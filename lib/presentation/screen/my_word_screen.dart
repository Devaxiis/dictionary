import 'package:dictionary/library.dart';

class MyWordScreen extends StatelessWidget {
  const MyWordScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("My Word"),
      ),
    );
  }
}
